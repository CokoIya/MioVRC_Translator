# UI
customtkinter>=5.2.0

# Audio
sounddevice>=0.4.6
numpy>=1.24.0
webrtcvad>=2.0.10

# ASR — faster-whisper (base and small only; models are pre-bundled)
faster-whisper>=1.0.0

# Translation backends
openai>=1.30.0        # covers OpenAI / DeepSeek / Qianwen / Custom
anthropic>=0.28.0

# OSC
python-osc>=1.8.3

# Optional: GPU acceleration (uncomment if CUDA available)
# torch>=2.0.0+cu118

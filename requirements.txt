--extra-index-url https://download.pytorch.org/whl/cpu

# UI
PySide6>=6.8.0,<7.0.0
Pillow>=12.3.0,<13.0.0

# Audio
sounddevice>=0.4.6,<1.0.0
SoundCard>=0.4.5,<1.0.0
PyAudioWPatch>=0.2.12,<1.0.0
numpy>=1.26.4,<2.0.0; python_version < "3.12"
numpy>=2.0.0; python_version >= "3.12"
webrtcvad-wheels>=2.0.14,<3.0.0
scipy>=1.10.0
soxr>=1.1.0,<2.0.0          # low-latency stateful capture resampling

# ASR - SenseVoiceSmall (Chinese / Cantonese)
funasr>=1.3.1,<2.0.0
modelscope>=1.28.0,<2.0.0
PyYAML>=6.0.2,<7.0.0
torch==2.8.0+cpu
torchaudio==2.8.0+cpu
editdistance>=0.8.1,<1.0.0

# ASR - Whisper runtime (default local model: Whisper Small)
openai-whisper>=20240930,<20270000

# ASR - shared HF download helpers
huggingface-hub>=1.16.1,<1.17.0  # Transformers 5.5 compatibility floor
tokenizers>=0.20.0,<1.0.0
tqdm>=4.66.0,<5.0.0

# Release builds should use CPU-only PyTorch. CUDA wheels pull several GB of
# CUDA/cuDNN DLLs into the installer.

# Translation backends
openai>=1.30.0,<2.0.0        # covers OpenAI / DeepSeek / Qianwen / Custom
anthropic>=0.28.0,<1.0.0
google-genai>=1.0.0,<2.0.0
# Qwen TTS DNS pinning subclasses the TLS-aware adapter hook added in 2.32.2.
requests>=2.32.2,<3.0.0
# Use the native Windows/macOS/Linux trust store for provider HTTPS. On
# Windows this also enables secure intermediate-certificate chain building.
truststore>=0.10.4,<1.0.0
cryptography>=48.0.1,<49.0.0  # Ed25519 update-manifest signing and verification
charset-normalizer>=3.4.0,<4.0.0  # bundled explicitly so requests has text detection in PyInstaller builds
aiohttp>=3.14.1,<4.0.0        # secure async HTTP floor used by API transports
idna>=3.15,<4.0               # URL/hostname validation security fixes
msgpack>=1.2.1,<2.0.0         # transitive runtime serialization security fixes
pyasn1>=0.6.4,<0.7.0          # fixes CVE-2026-59885 and CVE-2026-59886

# OSC
python-osc>=1.8.3,<2.0.0

# TTS (Text-to-Speech)
edge-tts>=7.2.0,<8.0.0       # Microsoft Edge TTS (primary)
gTTS>=2.3.0,<3.0.0           # Google TTS (works globally)
pyttsx3>=2.90,<3.0.0         # Offline TTS fallback
coqui-tts==0.27.5            # XTTS-v2 voice cloning runtime (publishes TTS.api)
scikit-learn>=1.8.0,<2.0.0   # Coqui XTTS runtime imports sklearn modules at startup
cutlet>=0.5.2,<0.6.0         # Japanese text preprocessing used by Coqui XTTS
fugashi>=1.5.2,<2.0.0        # MeCab wrapper required by Coqui/cutlet Japanese phonemizer
unidic-lite==1.0.8           # Bundled Japanese dictionary for fugashi in packaged builds
mojimoji>=0.0.13,<0.1.0      # cutlet dependency used during Japanese normalization
av>=12.0.0                   # PyAV for MP3 decoding (includes FFmpeg libraries)
style-bert-vits2==2.5.0      # Custom / Hololive local voices (supports Python 3.11+)
pyworld-prebuilt==0.3.5.post2  # Prebuilt WORLD vocoder required by Style-Bert-VITS2
transformers>=5.5.0,<6.0.0   # Shared by Style-Bert-VITS2 and Coqui XTTS; security floor
nltk>=3.10.0,<4.0.0          # Fixes CVE-2026-12243 path traversal in NLTK resource loading
sentencepiece>=0.2.0,<0.3.0  # English SBV2 DeBERTa tokenizer
protobuf>=5.0.0,<8.0.0       # SentencePiece model protobuf support
jieba>=0.42.1,<1.0.0         # Chinese SBV2 text segmentation
pypinyin>=0.55.0,<1.0.0      # Chinese SBV2 pronunciation conversion
cn2an>=0.5.23,<1.0.0         # Chinese SBV2 number normalization
g2p-en>=2.1.0,<3.0.0         # English SBV2 pronunciation conversion

# Optional: GPU acceleration (uncomment if CUDA available)
# torch>=2.4.0+cu118,<2.8.0

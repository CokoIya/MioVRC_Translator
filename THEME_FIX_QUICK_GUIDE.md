# 主题切换Bug修复 - 快速指南

## ✅ 修复完成

已修改 `src/ui_qt/styles.py` 中的两处 `shell_bg` 定义：

1. **第269行** - `build_main_window_styles` 函数
2. **第471行** - `build_settings_window_styles` 函数

## 🔧 修改内容

```python
# 修改前
shell_bg = "rgba(7, 9, 14, 0.18)" if is_dark else "rgba(247, 250, 255, 0.20)"

# 修改后
shell_bg = "transparent"
```

## 🚀 如何应用修复

### 方法1: 重启应用（推荐）

1. **关闭当前运行的应用**
2. **重新启动**:
   ```bash
   python main.py
   ```

### 方法2: 如果应用支持热重载

- 只需切换一次主题即可看到效果

## 🧪 验证修复

1. 启动应用后，点击右上角的**主题切换按钮**（月亮/太阳图标）
2. 切换到**深色主题**
3. 检查：
   - ✓ 背景应该是深色的
   - ✓ 所有白色文字应该清晰可见
   - ✓ 没有白色/浅灰色的中间层遮挡

## ❓ 如果还有问题

请告诉我：
1. 是否已经重启应用？
2. 主题切换按钮是否可以点击？
3. 切换后看到的颜色是什么样的？

---

**修复状态**: ✅ 代码已修改，等待重启应用验证
